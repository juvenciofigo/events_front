import React from "react";

export default function Checkout() {
    return (
        <div>
            <h2 className="text-2xl font-bold mb-4 text-text">Checkout</h2>
            <div className="p-6 bg-surface/50 backdrop-blur-xl border border-white/10 rounded-xl">
                <p className="text-muted">Formulário de checkout (assinatura / ticket) — em desenvolvimento</p>
            </div>
        </div>
    );
}
